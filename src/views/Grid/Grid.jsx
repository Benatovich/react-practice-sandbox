import React from 'react'

export default function Grid() {
  return (
    <>
      <div>
        <h1>Grid</h1>
        <ol>
          <li>Create a grid of cards/tiles with option to render in three different sizes (small, medium, large).</li>
          <li>Add button to randomly generate each tile size (within a set parameter). Render dimensions within each tile.</li>
          <li>Be able to sort tiles from largest to smallest and vice versa. </li>
        </ol>
      </div>
    </>  
  )
}
